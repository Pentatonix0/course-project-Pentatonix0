# Quiz Builder

Приложение для создания и прохождения тестов с вопросами и вариантами ответов.

## Возможности

-   CRUD для **Quiz** (тестов)
-   CRUD для **Questions** (вопросов)
-   CRUD для **Choices** (вариантов ответов)
-   Просмотр теста по ID: `/quizzes/{id}/preview`
-   Авторизация и аутентификация пользователей
-   Проверка владельца теста (owner-only)
-   Валидация типов данных
-   Прохождение теста локально и сохранение результатов

## Технологии

-   **Python 3.12+**
-   **FastAPI**
-   **SQLite / PostgreSQL**
-   **Pytest** для тестов
-   **pre-commit** (Black, isort, Ruff) для форматирования и линтинга

## Установка

```bash
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
pre-commit install
```

## Запуск

```bash
uvicorn app.main:app --reload
```

API будет доступен по адресу:

```
http://127.0.0.1:8000
```

## 🧪 Тестирование

```bash
pytest
```

## Безопасность

-   JWT аутентификация
-   Доступ к изменению тестов только для их владельца
-   Проверка типов данных и валидация входных данных
