# P11 - DAST evidence

This directory stores OWASP ZAP baseline outputs generated in CI.
Reports expected:
- zap_baseline.html
- zap_baseline.json

Download the `p11-dast-zap` artifact from Actions after a run and copy the reports here for PR evidence.
